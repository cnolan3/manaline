[package]
name = "manaline-cards"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "The manaline card set: card definitions, loader, and starter decklists"

[lib]
name = "cards"
path = "src/lib.rs"

[dependencies]
include_dir = "0.7.4"
manaline-cardir = { version = "0.1.0", path = "../cardir" }
manaline-engine = { path = "../engine" }
manaline-deckstats = { path = "../deckstats" }
dirs = "6"
