[package]
name = "manaline-cardsearch"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Scryfall-style card search over the manaline card database"

[lib]
name = "cardsearch"
path = "src/lib.rs"

[dependencies]
manaline-engine = { path = "../engine" }
manaline-carddb = { path = "../carddb" }
serde.workspace = true

[dev-dependencies]
manaline-cards = { path = "../cards" }
serde_json.workspace = true
